<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>SKYLINE — First-Person Flight</title>
<style>
  :root{--ink:#edf1f3;--muted:#aab3b8;--line:rgba(255,255,255,.18);--panel:rgba(8,12,15,.82);--accent:#dfe7ea}
  *{box-sizing:border-box} html,body{margin:0;width:100%;height:100%;overflow:hidden;background:#05080b;color:var(--ink);font-family:Inter,Segoe UI,Arial,sans-serif}
  canvas{display:block} button,select,input{font:inherit}
  #game{position:fixed;inset:0} #vignette,#cloudVeil,#glare{position:fixed;inset:0;pointer-events:none}
  #vignette{background:radial-gradient(circle at 50% 46%,transparent 45%,rgba(0,0,0,.18) 78%,rgba(0,0,0,.52) 125%);z-index:5}
  #cloudVeil{z-index:6;opacity:0;background:radial-gradient(circle at 50% 45%,rgba(255,255,255,.48),rgba(220,229,233,.76));mix-blend-mode:screen;transition:opacity .18s linear}
  #glare{z-index:7;opacity:0;background:radial-gradient(circle at var(--gx,70%) var(--gy,20%),rgba(255,249,222,.40) 0,rgba(255,241,197,.10) 7%,transparent 24%);mix-blend-mode:screen}
  .screen{position:fixed;inset:0;z-index:30;display:flex;align-items:center;justify-content:center;background:linear-gradient(120deg,rgba(3,7,10,.82),rgba(10,17,22,.44));backdrop-filter:blur(8px)}
  .hero{width:min(560px,90vw);padding:44px;border:1px solid var(--line);background:linear-gradient(145deg,rgba(12,18,22,.88),rgba(5,8,11,.72));box-shadow:0 30px 90px rgba(0,0,0,.45)}
  .eyebrow{font-size:11px;letter-spacing:.44em;color:#aeb8bc;margin-bottom:13px}.title{font-size:clamp(52px,9vw,96px);font-weight:300;letter-spacing:.14em;line-height:.9;margin:0 0 15px}.subtitle{font-size:12px;letter-spacing:.32em;color:#bdc6ca;margin-bottom:38px}
  .menuButtons{display:grid;gap:10px}.menuBtn,.smallBtn{border:1px solid rgba(255,255,255,.22);background:rgba(255,255,255,.035);color:white;padding:14px 17px;letter-spacing:.18em;font-size:11px;text-align:left;cursor:pointer;transition:.2s}.menuBtn:hover,.smallBtn:hover{background:rgba(255,255,255,.12);border-color:rgba(255,255,255,.48)}
  .footnote{margin-top:28px;color:#879197;font-size:11px;line-height:1.6}.hidden{display:none!important}
  .modal{position:fixed;inset:0;z-index:40;display:flex;align-items:center;justify-content:center;background:rgba(2,5,7,.74);backdrop-filter:blur(9px)}.modalCard{width:min(650px,92vw);max-height:85vh;overflow:auto;padding:30px;border:1px solid var(--line);background:rgba(9,14,18,.96)}
  .modal h2{font-weight:400;letter-spacing:.18em;font-size:20px;margin:0 0 25px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.key{display:flex;justify-content:space-between;border-bottom:1px solid rgba(255,255,255,.08);padding:10px 2px;color:#b9c1c5}.key b{color:white;font-weight:500}.settings{display:grid;gap:15px}.setting{display:grid;grid-template-columns:1fr 180px;align-items:center;gap:18px;color:#c8ced1}.setting select,.setting input{width:100%;accent-color:#d7dee1;background:#11181c;color:white;border:1px solid #364149;padding:8px}.modalActions{display:flex;justify-content:flex-end;margin-top:25px}
  #hud{position:fixed;z-index:12;inset:0;pointer-events:none;color:rgba(240,244,245,.92);text-shadow:0 1px 4px #000}.hudTop{position:absolute;top:20px;left:50%;transform:translateX(-50%);display:flex;gap:22px;padding:10px 15px;border-top:1px solid rgba(255,255,255,.22);background:linear-gradient(90deg,transparent,rgba(0,0,0,.25),transparent);font:11px/1.1 ui-monospace,SFMono-Regular,Consolas,monospace;letter-spacing:.08em}.readout{display:grid;gap:3px;min-width:84px}.readout span{font-size:8px;color:#aeb6ba}.readout b{font-weight:500}.hudBottom{position:absolute;bottom:18px;left:22px;font:10px ui-monospace,monospace;color:#aeb8bc;line-height:1.7}.statusDot{display:inline-block;width:6px;height:6px;border-radius:50%;background:#b9d5c2;box-shadow:0 0 6px rgba(160,220,180,.3);margin-right:7px}
  #warning{position:absolute;top:88px;left:50%;transform:translateX(-50%);font:600 12px ui-monospace,monospace;letter-spacing:.18em;color:#ffd0bd;opacity:0}.mouseHint{position:absolute;bottom:18px;right:22px;color:#9da7ac;font-size:10px;letter-spacing:.08em}
  #pause{background:rgba(3,6,8,.5)} #pause .hero{width:min(430px,88vw)}
  #loading{position:fixed;inset:0;z-index:50;display:flex;align-items:center;justify-content:center;background:#060a0d;letter-spacing:.28em;font-size:11px;color:#c8d0d3}.bar{position:absolute;width:180px;height:1px;background:#253036;margin-top:40px}.bar:after{content:"";display:block;width:55%;height:100%;background:#d7dddf;animation:load 1.3s ease-in-out infinite}@keyframes load{50%{transform:translateX(82px);width:45%}}
  @media(max-width:650px){.hero{padding:28px}.grid{grid-template-columns:1fr}.hudTop{gap:9px;width:96%;justify-content:center}.readout{min-width:55px}.setting{grid-template-columns:1fr 130px}}
</style>
<script type="importmap">
{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.185.1/build/three.module.js"}}
</script>
</head>
<body>
<div id="game"></div><div id="vignette"></div><div id="cloudVeil"></div><div id="glare"></div>
<div id="loading"><div>PREPARING AIRCRAFT</div><div class="bar"></div></div>
<div id="menu" class="screen hidden"><div class="hero"><div class="eyebrow">HIGH-ALTITUDE FREE FLIGHT</div><h1 class="title">SKYLINE</h1><div class="subtitle">FIRST-PERSON FLIGHT</div><div class="menuButtons"><button id="start" class="menuBtn">START FLIGHT</button><button data-open="controls" class="menuBtn">CONTROLS</button><button data-open="settings" class="menuBtn">SETTINGS</button></div><div class="footnote">An original real-time cockpit experience inspired by high-altitude aviation photography. Headphones recommended. Click the view to recapture the mouse after pausing.</div></div></div>
<div id="controls" class="modal hidden"><div class="modalCard"><h2>FLIGHT CONTROLS</h2><div class="grid"><div class="key"><span>Pitch up / down</span><b>W / S</b></div><div class="key"><span>Roll left / right</span><b>A / D</b></div><div class="key"><span>Yaw left / right</span><b>Q / E</b></div><div class="key"><span>Increase throttle</span><b>SHIFT</b></div><div class="key"><span>Decrease throttle</span><b>CTRL</b></div><div class="key"><span>Air brake</span><b>SPACE</b></div><div class="key"><span>Look around</span><b>MOUSE</b></div><div class="key"><span>Pause / release mouse</span><b>ESC</b></div></div><div class="modalActions"><button data-close class="smallBtn">CLOSE</button></div></div></div>
<div id="settings" class="modal hidden"><div class="modalCard"><h2>SETTINGS</h2><div class="settings"><label class="setting"><span>Graphics quality</span><select id="graphics"><option>Low</option><option selected>Medium</option><option>High</option></select></label><label class="setting"><span>Cloud quality</span><select id="cloudQuality"><option>Low</option><option selected>Medium</option><option>High</option></select></label><label class="setting"><span>Shadow quality</span><select id="shadowQuality"><option>Off</option><option selected>Medium</option><option>High</option></select></label><label class="setting"><span>Mouse sensitivity</span><input id="mouseSensitivity" type="range" min="0.25" max="1.5" value="0.72" step="0.01"></label><label class="setting"><span>Camera movement</span><input id="cameraSensitivity" type="range" min="0" max="1.5" value="0.65" step="0.01"></label><label class="setting"><span>Master volume</span><input id="masterVolume" type="range" min="0" max="1" value="0.65" step="0.01"></label><label class="setting"><span>Engine volume</span><input id="engineVolume" type="range" min="0" max="1" value="0.72" step="0.01"></label></div><div class="modalActions"><button data-close class="smallBtn">CLOSE</button></div></div></div>
<div id="pause" class="screen hidden"><div class="hero"><div class="eyebrow">FLIGHT PAUSED</div><h2 style="font-weight:300;letter-spacing:.15em">COCKPIT MENU</h2><div class="menuButtons"><button id="resume" class="menuBtn">RESUME FLIGHT</button><button data-open="controls" class="menuBtn">CONTROLS</button><button data-open="settings" class="menuBtn">SETTINGS</button><button id="restart" class="menuBtn">RESTART FLIGHT</button></div></div></div>
<div id="hud" class="hidden"><div class="hudTop"><div class="readout"><span>AIRSPEED</span><b id="hSpeed">000 KT</b></div><div class="readout"><span>ALTITUDE</span><b id="hAlt">00000 FT</b></div><div class="readout"><span>VERT SPEED</span><b id="hVs">+0000</b></div><div class="readout"><span>HEADING</span><b id="hHdg">000°</b></div><div class="readout"><span>THROTTLE</span><b id="hThr">00%</b></div></div><div id="warning">PULL UP</div><div class="hudBottom"><div><span class="statusDot"></span><span id="engineStatus">ENGINES NORMAL</span></div><div id="atmos">LOWER ATMOSPHERE</div></div><div class="mouseHint">MOUSE LOOK · ESC PAUSE</div></div>
<script type="module">
import * as THREE from 'three';

const $=s=>document.querySelector(s), clamp=THREE.MathUtils.clamp, lerp=THREE.MathUtils.lerp;
const game=$('#game'), menu=$('#menu'), pause=$('#pause'), hud=$('#hud'), loading=$('#loading');
let running=false, started=false, pointer=false, last=performance.now(), cloudOpacity=0, instrumentTimer=0;
const keys={}, settings={mouse:.72,camera:.65,master:.65,engine:.72,graphics:'Medium',clouds:'Medium',shadows:'Medium'};

const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance',logarithmicDepthBuffer:true});
renderer.setSize(innerWidth,innerHeight); renderer.setPixelRatio(Math.min(devicePixelRatio,1.5)); renderer.outputColorSpace=THREE.SRGBColorSpace;
renderer.toneMapping=THREE.ACESFilmicToneMapping; renderer.toneMappingExposure=1.06; renderer.shadowMap.enabled=true; renderer.shadowMap.type=THREE.PCFSoftShadowMap; game.appendChild(renderer.domElement);
const scene=new THREE.Scene(); scene.fog=new THREE.FogExp2(0xb9ced8,0.000008);
const camera=new THREE.PerspectiveCamera(68,innerWidth/innerHeight,.03,16000000); camera.position.set(0,1.58,.55);
const clock=new THREE.Clock();

const state={pos:new THREE.Vector3(0,5200,0),quat:new THREE.Quaternion(),speed:205,throttle:.68,verticalSpeed:0,rollRate:0,pitchRate:0,yawRate:0,airBrake:0,impact:0,time:0};
const initial=()=>{state.pos.set(0,5200,0);state.quat.identity();state.speed=205;state.throttle=.68;state.verticalSpeed=0;state.rollRate=state.pitchRate=state.yawRate=0;state.impact=0;lookYaw=lookPitch=0;};

// SKY — a continuous altitude-driven atmospheric shader.
const skyUniforms={uAltitude:{value:.1},uCloud:{value:0},uSun:{value:new THREE.Vector3(-.42,.55,-.72).normalize()}};
const skyMat=new THREE.ShaderMaterial({side:THREE.BackSide,depthWrite:false,uniforms:skyUniforms,vertexShader:`varying vec3 vDir;void main(){vDir=normalize(position);gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.0);}`,fragmentShader:`
precision highp float;varying vec3 vDir;uniform float uAltitude;uniform float uCloud;uniform vec3 uSun;
void main(){float y=normalize(vDir).y;float h=smoothstep(-.10,.22,y);float alt=clamp(uAltitude,0.,1.);vec3 lowTop=vec3(.075,.32,.62);vec3 highTop=vec3(.006,.018,.055);vec3 top=mix(lowTop,highTop,smoothstep(.16,.9,alt));vec3 horizon=mix(vec3(.60,.78,.91),vec3(.20,.39,.60),smoothstep(.25,.9,alt));float band=exp(-abs(y)*mix(8.,18.,alt));vec3 col=mix(horizon,top,pow(h,.48));col=mix(col,vec3(.82,.87,.88),band*(1.-alt)*.34);float sun=max(dot(normalize(vDir),uSun),0.);col+=vec3(1.0,.77,.42)*pow(sun,420.)*2.2;col+=vec3(1.0,.83,.58)*pow(sun,18.)*.13;col=mix(col,vec3(.78,.82,.84),uCloud*.68);gl_FragColor=vec4(col,1.);}`});
const skyGroup=new THREE.Group(); scene.add(skyGroup); const sky=new THREE.Mesh(new THREE.SphereGeometry(7000000,48,24),skyMat);sky.frustumCulled=false;skyGroup.add(sky);

function radialTexture(){const c=document.createElement('canvas');c.width=c.height=256;const x=c.getContext('2d'),g=x.createRadialGradient(128,128,1,128,128,126);g.addColorStop(0,'rgba(255,252,235,1)');g.addColorStop(.035,'rgba(255,248,220,.98)');g.addColorStop(.11,'rgba(255,235,185,.28)');g.addColorStop(.45,'rgba(255,220,160,.055)');g.addColorStop(1,'rgba(255,220,160,0)');x.fillStyle=g;x.fillRect(0,0,256,256);const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;return t}
const sunSprite=new THREE.Sprite(new THREE.SpriteMaterial({map:radialTexture(),transparent:true,depthWrite:false,blending:THREE.AdditiveBlending,toneMapped:false}));sunSprite.position.copy(skyUniforms.uSun.value).multiplyScalar(5000000);sunSprite.scale.setScalar(285000);skyGroup.add(sunSprite);

// Sparse stars become visible only near the top of the atmosphere.
const starGeo=new THREE.BufferGeometry(),starPos=[];for(let i=0;i<850;i++){const v=new THREE.Vector3().randomDirection().multiplyScalar(6200000);starPos.push(v.x,v.y,v.z)}starGeo.setAttribute('position',new THREE.Float32BufferAttribute(starPos,3));const starMat=new THREE.PointsMaterial({color:0xdce7ef,size:1700,sizeAttenuation:true,transparent:true,opacity:0,depthWrite:false});skyGroup.add(new THREE.Points(starGeo,starMat));

const world=new THREE.Group();scene.add(world);
// Curved Earth with original procedural ocean/land coloration.
const R=6371000;
const earthMat=new THREE.ShaderMaterial({uniforms:{uSun:{value:skyUniforms.uSun.value}},vertexShader:`varying vec3 vN;varying vec3 vP;void main(){vN=normalize(normal);vP=position;gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.);}`,fragmentShader:`precision highp float;varying vec3 vN;varying vec3 vP;uniform vec3 uSun;float hash(vec3 p){return fract(sin(dot(p,vec3(17.13,91.7,43.11)))*43758.5453);}void main(){vec3 n=normalize(vN);float continental=sin(n.x*13.+sin(n.z*7.))*sin(n.z*9.-n.y*5.)+sin(n.x*31.+n.z*17.)*.25;float land=smoothstep(.30,.54,continental);float ice=smoothstep(.86,.98,abs(n.y));vec3 ocean=mix(vec3(.018,.105,.16),vec3(.035,.19,.23),max(n.y,0.));vec3 ground=mix(vec3(.18,.20,.12),vec3(.31,.30,.18),hash(floor(n*130.)));vec3 col=mix(ocean,ground,land);col=mix(col,vec3(.80,.84,.82),ice);float light=.34+.66*max(dot(n,uSun),0.);col*=light;float glint=pow(max(dot(reflect(-uSun,n),vec3(0.,1.,0.)),0.),70.)*(1.-land);col+=vec3(1.,.82,.52)*glint*.65;gl_FragColor=vec4(col,1.);}`});
const earth=new THREE.Mesh(new THREE.SphereGeometry(R,128,64),earthMat);earth.position.y=-R;world.add(earth);

// Distant mountain silhouettes near the starting region.
const mountains=new THREE.Group();world.add(mountains);const mountainMat=new THREE.MeshStandardMaterial({color:0x59645d,roughness:1});for(let i=0;i<75;i++){const a=Math.random()*Math.PI*2,d=25000+Math.random()*110000,h=500+Math.random()*2100,r=h*(1.2+Math.random());const m=new THREE.Mesh(new THREE.ConeGeometry(r,h,6),mountainMat);m.position.set(Math.cos(a)*d,h/2-8,Math.sin(a)*d);m.rotation.y=Math.random()*Math.PI;m.scale.z=.55+Math.random();mountains.add(m)}

// Instanced procedural cloud puffs: few draw calls, many varied cloud forms.
const cloudClusters=[];const maxPuffs=760;const puffGeo=new THREE.IcosahedronGeometry(1,2);const cloudMat=new THREE.MeshLambertMaterial({color:0xf1f3f0,transparent:true,opacity:.78,depthWrite:false,roughness:1});const cloudMesh=new THREE.InstancedMesh(puffGeo,cloudMat,maxPuffs);cloudMesh.frustumCulled=false;world.add(cloudMesh);const dummy=new THREE.Object3D();let puff=0;
function addCloudLayer(count,altMin,altMax,spread,sizeMin,sizeMax){for(let i=0;i<count;i++){const cx=(Math.random()-.5)*spread,cz=(Math.random()-.5)*spread,cy=lerp(altMin,altMax,Math.random());const radius=lerp(sizeMin,sizeMax,Math.random());cloudClusters.push({x:cx,y:cy,z:cz,r:radius});const pieces=3+Math.floor(Math.random()*4);for(let j=0;j<pieces&&puff<maxPuffs;j++){dummy.position.set(cx+(Math.random()-.5)*radius*1.5,cy+(Math.random()-.5)*radius*.28,cz+(Math.random()-.5)*radius);dummy.scale.set(radius*(.55+Math.random()*.55),radius*(.20+Math.random()*.22),radius*(.55+Math.random()*.6));dummy.rotation.set(Math.random(),Math.random(),Math.random());dummy.updateMatrix();cloudMesh.setMatrixAt(puff++,dummy.matrix)}}}
addCloudLayer(100,1800,3300,290000,700,2100);addCloudLayer(36,6500,8200,330000,850,2500);addCloudLayer(28,10500,12500,370000,700,1900);cloudMesh.count=puff;cloudMesh.instanceMatrix.needsUpdate=true;

const hemi=new THREE.HemisphereLight(0xbfd9ef,0x31383a,1.15);scene.add(hemi);const sunLight=new THREE.DirectionalLight(0xfff1d2,3.1);sunLight.castShadow=true;sunLight.shadow.mapSize.set(1024,1024);sunLight.shadow.camera.left=-12;sunLight.shadow.camera.right=12;sunLight.shadow.camera.top=12;sunLight.shadow.camera.bottom=-12;sunLight.shadow.camera.near=.1;sunLight.shadow.camera.far=80;scene.add(sunLight);scene.add(sunLight.target);

// COCKPIT AND VISIBLE AIRCRAFT — built from efficient original geometry.
const cockpit=new THREE.Group();scene.add(cockpit);const dark=new THREE.MeshStandardMaterial({color:0x111517,roughness:.72,metalness:.14});const darker=new THREE.MeshStandardMaterial({color:0x07090a,roughness:.62,metalness:.22});const trim=new THREE.MeshStandardMaterial({color:0x343b3e,roughness:.48,metalness:.55});const pale=new THREE.MeshStandardMaterial({color:0xc4c9c8,roughness:.45,metalness:.35});
function box(n,s,p,mat=dark){const o=new THREE.Mesh(new THREE.BoxGeometry(...s),mat);o.name=n;o.position.set(...p);o.castShadow=o.receiveShadow=true;cockpit.add(o);return o}
box('floor',[3.2,.12,5],[0,.02,-.45],darker);box('dash',[3.0,.72,.52],[0,.83,-1.62]);box('panel',[2.65,.75,.12],[0,.75,-1.91],darker);box('glareshield',[3.05,.13,.72],[0,1.25,-1.64],dark);box('leftwall',[.17,1.55,3.7],[-1.52,.72,-.2]);box('rightwall',[.17,1.55,3.7],[1.52,.72,-.2]);box('ceiling',[3.2,.12,2.6],[0,2.45,-.45],dark);
// Windshield and side-window structural frame.
box('centerpost',[.11,1.18,.13],[0,1.82,-2.02],trim);box('leftpost',[.15,1.45,.16],[-1.36,1.75,-1.69],trim).rotation.z=-.16;box('rightpost',[.15,1.45,.16],[1.36,1.75,-1.69],trim).rotation.z=.16;box('topframe',[2.8,.15,.18],[0,2.34,-1.62],trim);box('leftsill',[.18,.18,2.45],[-1.43,1.28,-.42],trim);box('rightsill',[.18,.18,2.45],[1.43,1.28,-.42],trim);
// Console and throttle quadrant.
box('pedestal',[.72,.73,1.7],[0,.43,-.52],dark);const throttleLevers=[];for(let i=0;i<2;i++){const g=new THREE.Group();const stem=new THREE.Mesh(new THREE.CylinderGeometry(.035,.045,.48,10),trim);stem.rotation.x=Math.PI/2;stem.position.z=-.18;const grip=new THREE.Mesh(new THREE.BoxGeometry(.16,.12,.22),darker);grip.position.z=-.43;g.add(stem,grip);g.position.set((i?1:-1)*.12,.82,-.58);g.rotation.x=-.45;cockpit.add(g);throttleLevers.push(g)}
// Yokes.
function makeYoke(x){const g=new THREE.Group();const shaft=new THREE.Mesh(new THREE.CylinderGeometry(.035,.045,.58,12),trim);shaft.rotation.x=Math.PI/2;const wheel=new THREE.Mesh(new THREE.TorusGeometry(.25,.035,10,28,Math.PI*1.55),darker);wheel.rotation.z=.7;wheel.position.z=-.32;const hub=new THREE.Mesh(new THREE.BoxGeometry(.26,.13,.09),darker);hub.position.z=-.32;g.add(shaft,wheel,hub);g.position.set(x,.72,-1.3);cockpit.add(g);return g}const yoke=makeYoke(-.62);makeYoke(.62);
// Overhead panel and small switches.
box('overhead',[1.65,.12,.85],[0,2.30,-.55],darker).rotation.x=.12;for(let i=0;i<18;i++){const sw=new THREE.Mesh(new THREE.BoxGeometry(.035,.035,.09),i%4===0?new THREE.MeshStandardMaterial({color:0x8c6e3b,emissive:0x3a2308,emissiveIntensity:.5}):trim);sw.position.set((i%6-2.5)*.18,2.22,-.78+Math.floor(i/6)*.20);sw.rotation.x=.12;cockpit.add(sw)}
// Seats glimpsed while looking sideways/back.
for(const x of [-.72,.72]){box('seat',[.62,.18,.72],[x,.34,.72],dark);const b=box('seatback',[.66,1.15,.18],[x,.89,1.02],dark);b.rotation.x=-.1}
// Exterior nose, wings, and engines visible through windows; no external camera exists.
const aircraftMat=new THREE.MeshStandardMaterial({color:0xb9c0c2,metalness:.62,roughness:.38});const nose=new THREE.Mesh(new THREE.CapsuleGeometry(.92,6.6,12,26),aircraftMat);nose.rotation.x=Math.PI/2;nose.position.set(0,.73,-4.6);cockpit.add(nose);const wingGeo=new THREE.BufferGeometry();wingGeo.setAttribute('position',new THREE.Float32BufferAttribute([0,0,0,-10,0,2,-10,0,3.2,0,0,1.1, 0,0,0,0,0,1.1,10,0,3.2,10,0,2],3));wingGeo.setIndex([0,1,2,0,2,3,4,5,6,4,6,7]);wingGeo.computeVertexNormals();const wings=new THREE.Mesh(wingGeo,aircraftMat);wings.position.set(0,.35,.45);cockpit.add(wings);for(const x of [-4.0,4.0]){const eng=new THREE.Mesh(new THREE.CylinderGeometry(.48,.64,1.8,24,1,true),aircraftMat);eng.rotation.x=Math.PI/2;eng.position.set(x,.02,.9);cockpit.add(eng);const fan=new THREE.Mesh(new THREE.CircleGeometry(.46,24),new THREE.MeshStandardMaterial({color:0x171b1d,metalness:.7,roughness:.25}));fan.position.set(x,.02,-.02);cockpit.add(fan)}

// Live multifunction displays.
function screenTexture(w=512,h=512){const c=document.createElement('canvas');c.width=w;c.height=h;const t=new THREE.CanvasTexture(c);t.colorSpace=THREE.SRGBColorSpace;return {c,x:c.getContext('2d'),t}}
const pfd=screenTexture(),engDisplay=screenTexture(384,512);
function addScreen(tex,size,pos){const mat=new THREE.MeshStandardMaterial({map:tex.t,emissive:0xffffff,emissiveMap:tex.t,emissiveIntensity:.62,roughness:.45});const m=new THREE.Mesh(new THREE.PlaneGeometry(...size),mat);m.position.set(...pos);cockpit.add(m);return m}addScreen(pfd,[.68,.62],[-.56,.82,-1.985]);addScreen(engDisplay,[.52,.62],[.45,.82,-1.985]);
function drawPFD(){const x=pfd.x,w=512,h=512;x.fillStyle='#071013';x.fillRect(0,0,w,h);const e=new THREE.Euler().setFromQuaternion(state.quat,'YXZ'),pitch=e.x,roll=e.z;x.save();x.translate(w/2,h/2);x.rotate(roll);const py=pitch*500;x.fillStyle='#2c668c';x.fillRect(-600,-600+py,1200,600);x.fillStyle='#755c3a';x.fillRect(-600,py,1200,600);x.strokeStyle='rgba(255,255,255,.8)';x.lineWidth=4;x.beginPath();x.moveTo(-600,py);x.lineTo(600,py);x.stroke();for(let d=-20;d<=20;d+=5){if(!d)continue;const yy=py-d*8;x.lineWidth=2;x.beginPath();x.moveTo(-45,yy);x.lineTo(45,yy);x.stroke()}x.restore();x.strokeStyle='#f3f5f2';x.lineWidth=4;x.beginPath();x.moveTo(170,260);x.lineTo(225,260);x.lineTo(256,280);x.lineTo(287,260);x.lineTo(342,260);x.stroke();x.font='24px monospace';x.fillStyle='#e9f0ec';x.fillText(String(Math.round(state.speed*1.94384)).padStart(3,'0'),28,260);x.fillText(String(Math.round(state.pos.y*3.28084)).padStart(5,'0'),390,260);x.font='15px monospace';x.fillStyle='#9bc7d8';x.fillText('SPD',28,225);x.fillText('ALT',440,225);pfd.t.needsUpdate=true}
function drawEngine(){const x=engDisplay.x,w=384,h=512;x.fillStyle='#081012';x.fillRect(0,0,w,h);x.fillStyle='#b9c7c5';x.font='18px monospace';x.fillText('ENGINE',22,35);for(let i=0;i<2;i++){const cx=105+i*175,cy=150;x.strokeStyle='#526165';x.lineWidth=12;x.beginPath();x.arc(cx,cy,58,Math.PI*.8,Math.PI*2.2);x.stroke();x.strokeStyle='#b8d7c8';x.beginPath();x.arc(cx,cy,58,Math.PI*.8,Math.PI*(.8+1.4*state.throttle));x.stroke();x.fillStyle='#e7ece9';x.font='24px monospace';x.fillText(Math.round(state.throttle*100)+'%',cx-28,cy+8)}x.font='16px monospace';x.fillStyle='#aab9b6';x.fillText('N1',25,235);x.fillText((state.throttle*92).toFixed(1),160,235);x.fillText('EGT',25,275);x.fillText(Math.round(390+state.throttle*410)+' C',160,275);x.fillText('FUEL FLOW',25,315);x.fillText((.5+state.throttle*2.4).toFixed(2),200,315);x.fillStyle='#8ebca0';x.fillText('SYSTEMS NORMAL',90,448);engDisplay.t.needsUpdate=true}
drawPFD();drawEngine();

// Procedural Web Audio: engine harmonics, filtered wind, cockpit vibration and button chirps.
let audio=null;
function initAudio(){if(audio){audio.ctx.resume();return}const ctx=new (window.AudioContext||window.webkitAudioContext)(),master=ctx.createGain(),engineGain=ctx.createGain(),windGain=ctx.createGain();master.gain.value=settings.master;engineGain.gain.value=.08*settings.engine;windGain.gain.value=.025;engineGain.connect(master);windGain.connect(master);master.connect(ctx.destination);const oscs=[];[42,84,126].forEach((f,i)=>{const o=ctx.createOscillator(),g=ctx.createGain();o.type=i?'sine':'sawtooth';o.frequency.value=f;g.gain.value=[.7,.25,.12][i];o.connect(g).connect(engineGain);o.start();oscs.push(o)});const buffer=ctx.createBuffer(1,ctx.sampleRate*2,ctx.sampleRate),d=buffer.getChannelData(0);for(let i=0;i<d.length;i++)d[i]=Math.random()*2-1;const noise=ctx.createBufferSource(),filter=ctx.createBiquadFilter();noise.buffer=buffer;noise.loop=true;filter.type='bandpass';filter.frequency.value=680;filter.Q.value=.6;noise.connect(filter).connect(windGain);noise.start();audio={ctx,master,engineGain,windGain,filter,oscs}}
function beep(freq=620,dur=.06){if(!audio)return;const o=audio.ctx.createOscillator(),g=audio.ctx.createGain();o.frequency.value=freq;o.type='sine';g.gain.setValueAtTime(.035,audio.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,audio.ctx.currentTime+dur);o.connect(g).connect(audio.master);o.start();o.stop(audio.ctx.currentTime+dur)}
function updateAudio(){if(!audio)return;audio.master.gain.setTargetAtTime(settings.master,audio.ctx.currentTime,.08);audio.engineGain.gain.setTargetAtTime((.035+.09*state.throttle)*settings.engine,audio.ctx.currentTime,.1);audio.windGain.gain.setTargetAtTime(.012+Math.max(0,state.speed-100)/4200,audio.ctx.currentTime,.1);audio.filter.frequency.setTargetAtTime(380+state.speed*2.2,audio.ctx.currentTime,.12);audio.oscs.forEach((o,i)=>o.frequency.setTargetAtTime((34+state.throttle*30)*(i+1),audio.ctx.currentTime,.1))}

let lookYaw=0,lookPitch=0;
document.addEventListener('mousemove',e=>{if(!pointer||!running)return;const s=settings.mouse*.00175;lookYaw=clamp(lookYaw-e.movementX*s,-2.25,2.25);lookPitch=clamp(lookPitch-e.movementY*s,-1.05,.82)});
document.addEventListener('keydown',e=>{keys[e.code]=true;if(['Space','ControlLeft','ControlRight','ShiftLeft','ShiftRight'].includes(e.code))e.preventDefault()});document.addEventListener('keyup',e=>keys[e.code]=false);
document.addEventListener('pointerlockchange',()=>{pointer=document.pointerLockElement===renderer.domElement;if(started&&!pointer&&running){running=false;pause.classList.remove('hidden')}});
renderer.domElement.addEventListener('click',()=>{if(started&&!pointer&&!running){running=true;pause.classList.add('hidden');renderer.domElement.requestPointerLock()}});

function openModal(id){beep(520);$('#'+id).classList.remove('hidden')}document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>openModal(b.dataset.open));document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>b.closest('.modal').classList.add('hidden'));
function startFlight(){initAudio();beep();started=running=true;menu.classList.add('hidden');pause.classList.add('hidden');hud.classList.remove('hidden');renderer.domElement.requestPointerLock()}
$('#start').onclick=startFlight;$('#resume').onclick=()=>{running=true;pause.classList.add('hidden');renderer.domElement.requestPointerLock()};$('#restart').onclick=()=>{initial();startFlight()};

function applySettings(){settings.graphics=$('#graphics').value;settings.clouds=$('#cloudQuality').value;settings.shadows=$('#shadowQuality').value;settings.mouse=+$('#mouseSensitivity').value;settings.camera=+$('#cameraSensitivity').value;settings.master=+$('#masterVolume').value;settings.engine=+$('#engineVolume').value;const ratios={Low:1,Medium:1.5,High:2};renderer.setPixelRatio(Math.min(devicePixelRatio,ratios[settings.graphics]));cloudMesh.count=Math.min(puff,{Low:310,Medium:540,High:maxPuffs}[settings.clouds]);renderer.shadowMap.enabled=settings.shadows!=='Off';const sz=settings.shadows==='High'?2048:1024;sunLight.shadow.mapSize.set(sz,sz);sunLight.shadow.map?.dispose()}
document.querySelectorAll('#settings input,#settings select').forEach(el=>el.addEventListener('input',applySettings));

const forward=new THREE.Vector3(),up=new THREE.Vector3(),right=new THREE.Vector3(),invQ=new THREE.Quaternion(),tmpQ=new THREE.Quaternion(),axis=new THREE.Vector3();
function updateFlight(dt){state.time+=dt;const pitchIn=(keys.KeyW?1:0)-(keys.KeyS?1:0),rollIn=(keys.KeyA?1:0)-(keys.KeyD?1:0),yawIn=(keys.KeyQ?1:0)-(keys.KeyE?1:0);if(keys.ShiftLeft||keys.ShiftRight)state.throttle+=dt*.16;if(keys.ControlLeft||keys.ControlRight)state.throttle-=dt*.16;state.throttle=clamp(state.throttle,.08,1);state.airBrake=lerp(state.airBrake,keys.Space?1:0,1-Math.exp(-dt*5));const authority=clamp(state.speed/145,.45,1.2);state.pitchRate=lerp(state.pitchRate,pitchIn*.28*authority,1-Math.exp(-dt*2.1));state.rollRate=lerp(state.rollRate,rollIn*.48*authority,1-Math.exp(-dt*2.4));state.yawRate=lerp(state.yawRate,yawIn*.18*authority,1-Math.exp(-dt*1.8));
  // Gentle aerodynamic self-stability, without arcade snapping.
  const e=new THREE.Euler().setFromQuaternion(state.quat,'YXZ');if(!rollIn)state.rollRate+=clamp(-e.z*.11,-.045,.045)*dt;if(!pitchIn)state.pitchRate+=clamp(-e.x*.035,-.018,.018)*dt;
  tmpQ.identity();tmpQ.multiply(new THREE.Quaternion().setFromAxisAngle(axis.set(1,0,0),state.pitchRate*dt));tmpQ.multiply(new THREE.Quaternion().setFromAxisAngle(axis.set(0,1,0),state.yawRate*dt));tmpQ.multiply(new THREE.Quaternion().setFromAxisAngle(axis.set(0,0,1),state.rollRate*dt));state.quat.multiply(tmpQ).normalize();
  const target=92+state.throttle*250-state.airBrake*92;state.speed+=clamp(target-state.speed,-38,25)*dt*.22;state.speed=clamp(state.speed,72,355);
  forward.set(0,0,-1).applyQuaternion(state.quat);up.set(0,1,0).applyQuaternion(state.quat);right.set(1,0,0).applyQuaternion(state.quat);const bankLoss=1-Math.abs(up.y);const sink=3.0+bankLoss*18+Math.max(0,115-state.speed)*.09;const oldY=state.pos.y;state.pos.addScaledVector(forward,state.speed*dt);state.pos.y-=sink*dt;state.verticalSpeed=(state.pos.y-oldY)/Math.max(dt,.001);
  if(state.pos.y<24){state.pos.y=24;state.impact=Math.min(1,state.impact+dt*5);state.verticalSpeed=Math.max(0,state.verticalSpeed);state.quat.slerp(new THREE.Quaternion().setFromEuler(new THREE.Euler(0,e.y,0)),dt*1.8);state.speed*=Math.pow(.94,dt)}else state.impact=Math.max(0,state.impact-dt*2);
}

function cloudDensity(){let dens=0;for(const c of cloudClusters){const dx=state.pos.x-c.x,dz=state.pos.z-c.z,dy=Math.abs(state.pos.y-c.y);if(dy<c.r*.55){const d=Math.sqrt(dx*dx+dz*dz);if(d<c.r*1.25)dens=Math.max(dens,(1-d/(c.r*1.25))*(1-dy/(c.r*.55)))}}return clamp(dens,0,1)}
function updateWorld(dt){invQ.copy(state.quat).invert();world.quaternion.copy(invQ);world.position.copy(state.pos).multiplyScalar(-1).applyQuaternion(invQ);skyGroup.quaternion.copy(invQ);const lightDir=skyUniforms.uSun.value.clone().applyQuaternion(invQ);sunLight.position.copy(lightDir).multiplyScalar(28);sunLight.target.position.set(0,0,-3);const altN=clamp((state.pos.y-5000)/26000,0,1);skyUniforms.uAltitude.value=altN;starMat.opacity=smoothstep(.62,.98,altN)*.72;const dens=cloudDensity();cloudOpacity=lerp(cloudOpacity,dens,1-Math.exp(-dt*2.2));skyUniforms.uCloud.value=cloudOpacity;$('#cloudVeil').style.opacity=(cloudOpacity*.82).toFixed(2);scene.fog.density=lerp(.000008,.00012,cloudOpacity);scene.fog.color.setRGB(lerp(.70,.83,cloudOpacity),lerp(.79,.86,cloudOpacity),lerp(.83,.87,cloudOpacity));hemi.intensity=1.15-cloudOpacity*.38;sunLight.intensity=3.1-cloudOpacity*1.55;
  // Subtle sun glare only while the sun is in front of the pilot.
  const sp=sunSprite.getWorldPosition(new THREE.Vector3()).project(camera),visible=sp.z<1&&Math.abs(sp.x)<1.2&&Math.abs(sp.y)<1.2;const gaze=Math.max(0,1-Math.hypot(sp.x,sp.y)*.9);$('#glare').style.setProperty('--gx',((sp.x*.5+.5)*100)+'%');$('#glare').style.setProperty('--gy',((-sp.y*.5+.5)*100)+'%');$('#glare').style.opacity=visible?(gaze*.42*(1-cloudOpacity)).toFixed(2):0;
}
function smoothstep(a,b,x){x=clamp((x-a)/(b-a),0,1);return x*x*(3-2*x)}
function updateCamera(dt){const turbulence=(Math.sin(state.time*2.1)+Math.sin(state.time*5.7)*.45+Math.sin(state.time*13.3)*.12)*.0025*settings.camera;const hard=Math.abs(state.rollRate)*.025+Math.abs(state.pitchRate)*.018;const accel=(state.throttle-.55)*.012;const shake=state.impact*.035;camera.position.set(turbulence+Math.sin(state.time*37)*shake,1.58+turbulence*.7+Math.abs(Math.sin(state.time*29))*shake,.55+accel+Math.cos(state.time*31)*shake);camera.quaternion.setFromEuler(new THREE.Euler(lookPitch+turbulence,lookYaw,turbulence*.7-state.rollRate*hard,'YXZ'));yoke.rotation.z=lerp(yoke.rotation.z,state.rollRate*1.7,dt*5);yoke.rotation.x=lerp(yoke.rotation.x,-state.pitchRate*1.2,dt*5);throttleLevers.forEach(g=>g.rotation.x=-.78+state.throttle*.63)}
function updateHUD(){const kts=Math.round(state.speed*1.94384),ft=Math.round(state.pos.y*3.28084),fpm=Math.round(state.verticalSpeed*196.85/10)*10;forward.set(0,0,-1).applyQuaternion(state.quat);let hdg=(Math.atan2(-forward.x,-forward.z)*180/Math.PI+360)%360;$('#hSpeed').textContent=String(kts).padStart(3,'0')+' KT';$('#hAlt').textContent=String(ft).padStart(5,'0')+' FT';$('#hVs').textContent=(fpm>=0?'+':'')+String(fpm).padStart(4,'0');$('#hHdg').textContent=String(Math.round(hdg)).padStart(3,'0')+'°';$('#hThr').textContent=Math.round(state.throttle*100)+'%';const warn=state.pos.y<280&&state.verticalSpeed<-5;$('#warning').style.opacity=warn?1:0;$('#engineStatus').textContent=state.airBrake>.3?'AIR BRAKE DEPLOYED':'ENGINES NORMAL';$('#atmos').textContent=state.pos.y<7000?'LOWER ATMOSPHERE':state.pos.y<16000?'UPPER TROPOSPHERE':state.pos.y<26000?'LOWER STRATOSPHERE':'EDGE OF THE STRATOSPHERE'}

function animate(now){requestAnimationFrame(animate);const dt=Math.min(.033,(now-last)/1000||.016);last=now;if(running)updateFlight(dt);updateWorld(dt);updateCamera(dt);updateAudio();instrumentTimer+=dt;if(instrumentTimer>.08){instrumentTimer=0;drawPFD();drawEngine();updateHUD()}renderer.render(scene,camera)}
window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
setTimeout(()=>{loading.classList.add('hidden');menu.classList.remove('hidden')},900);applySettings();animate(performance.now());
</script>
</body>
</html>